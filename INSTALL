Build on Mac OS X
==================================
1. install qwt ( libqwt.dylib has to be in /usr/lib )
2. qmake -spec macx-g++ activitydiary.pro
3. make
4. mv icons to /usr/share/activitydiary/icons

Build on Linux
=================================
1. qwt header are searched in /usr/include/qwt
2. qwt lib must be named libqwt.so or libqwt-qt4.so
3. mkdir build && cd build && qmake ../activitydiary.pro
3. make
4. make install
